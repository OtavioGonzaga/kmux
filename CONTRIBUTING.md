# Contributing

- Branches and pull requests target `main`.
- Pull requests do not change `Cargo.toml` package version.
- Public changes update `CHANGELOG.md` under `[Unreleased]`.
- Run formatting, Clippy, and tests before requesting review.
- Releases are created only through the **Create Release** GitHub Actions workflow.
